apiVersion: v1
kind: Service
metadata:
  name: workload-variant-autoscaler-vllm
  labels:
    llm-d.ai/model: Qwen3-32B
spec:
  selector:
    llm-d.ai/model: Qwen3-32B
    llm-d.ai/role: decode
  ports:
    - name: vllm
      port: 8000
      targetPort: modelserver
      protocol: TCP
  type: ClusterIP
